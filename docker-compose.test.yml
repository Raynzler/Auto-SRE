# Full test suite runner.
#   docker compose -f docker-compose.test.yml up --build --exit-code-from tests
# Tests run in-process (FastAPI TestClient), so no other services need to be up.
services:
  tests:
    build:
      context: .
      dockerfile: tests/Dockerfile
    command: ["pytest"]
